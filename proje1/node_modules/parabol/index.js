module.exports = function(a,b,c) {
  if (a === 0) {
    return; //başkatsayının 0 olmamasına dikkat et.
  }
  var r = - b/(2 * a);
  var k = a * r * r + b * r + c;
  
  return "(" + r + "," + k + ")";
}
